{
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": ["-y", "@playwright/mcp", "--isolate"]
    },
    "centy": {
      "args": [
        "-y",
        "centy-mcp"
      ],
      "command": "npx"
    },
    "ameliso": {
      "command": "cargo",
      "args": ["run", "--quiet", "--package", "ameliso-mcp", "--"]
    }
  }
}
