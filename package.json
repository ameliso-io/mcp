{
  "name": "ameliso",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "prepare": "husky",
    "build": "cd web && pnpm build",
    "dev": "concurrently --names \"grpc,web\" --prefix-colors \"blue,green\" \"cargo run -p ameliso-server\" \"cd web && pnpm dev\""
  },
  "packageManager": "pnpm@10.33.0",
  "devDependencies": {
    "concurrently": "^9.2.1",
    "husky": "^9.1.7"
  }
}
