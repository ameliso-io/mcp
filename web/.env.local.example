# URL of the Ameliso gRPC-Web server.
# Defaults to empty string (proxied via Next.js rewrites to http://localhost:50052).
# Override in production to point directly at the deployed server.
NEXT_PUBLIC_API_URL=
